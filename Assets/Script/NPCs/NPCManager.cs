using BHSSolo.DungeonDefense.NPCs;
using BHSSolo.DungeonDefense.Singleton;
using System.Collections.Generic;
using UnityEngine;

namespace BHSSolo.DungeonDefense.Management
{
    public class NPCManager : SingletonMono<NPCManager>
    {
        //[SerializeField] private GameManager GameManager;

        //private List<EnemyController> _enemyControllers;
        //private List<AllyController> _allyControllers;

        //private void Awake()
        //{
        //    if (GameManager == null) { GameManager = GetComponent<GameManager>(); }
        //}
    }
}